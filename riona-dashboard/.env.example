# Riona Dashboard Environment Configuration

# Backend API URL (required)
# This should point to your Riona AI Agent backend server
NEXT_PUBLIC_RIONA_API=http://localhost:3000

# Optional: Custom API endpoints if your backend uses different paths
# NEXT_PUBLIC_CUSTOM_ENDPOINTS=true