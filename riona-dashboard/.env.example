# Riona Dashboard Environment Configuration

# Backend server URL (server-side only)
# This is where the proxy will forward requests
RIONA_BACKEND=http://localhost:3099

# Frontend API base (client-side)
# This points to our proxy endpoint
NEXT_PUBLIC_RIONA_API=/api/riona